apiVersion: v1
kind: Secret
metadata:
  name: casehub-db
type: Opaque
stringData:
  # Replace credentials, Service name, namespace and database before applying.
  DATABASE_URL: "postgres://casehub:REPLACE_ME@postgres.database.svc.cluster.local:5432/casehub?sslmode=disable"
