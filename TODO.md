# 待办

## 测试用例设计 MCP：仅接入 planner

范围确认（2026-09-11）：抽屉顶部可选择 MCP，当前测试设计服务只运行 `auto-test` 的 planner。抽屉展示探索与设计进度，产出待审核的测试计划和用例草稿；generator 后续考虑接入用例管理侧，healer 不在本次范围内。

参考框架：`../auto-test`。

- 现有 `.claude/agents/playwright-test-planner.md` 定义读取需求、复用探索记录、读取业务提示和历史问题、浏览器探索、设计场景、保存计划及用例表的流程。
- `.mcp.json` 启动的是 Playwright 工具服务；planner 本身是 Agent 指令，尚需封装为可调用的流程服务，并由 Agent 运行时执行。
- `review/server.mjs` 提供文档编辑和人工审核接口，目前没有启动 planner 或订阅运行进度的接口。

- [ ] 封装只执行 planner 的 MCP 业务入口，明确需求内容、目标环境、seed 和探索上下文的输入，以及计划、用例草稿和探索记录的输出；保留人工审核步骤。
- [ ] 增加任务状态和结构化进度事件，在抽屉展示读取需求、准备环境、探索页面、设计场景、保存草稿等阶段，以及已观察的操作结果、产出数量和失败原因。
- [ ] 后端保存任务及事件，通过 SSE 等机制向抽屉推送；重新打开抽屉可恢复查看已有任务，停止操作传递到执行流程。

## 后续：用例关联需求，再接入 generator

- [ ] 为 CaseHub 用例建立与需求文档的持久关联，支持查看对应需求。`auto-test/test-model.md` 已有 `request`（需求 ID），planner 已要求在用例表中填写；接入时保留该关系。目前 CaseHub 的 `TestCase` 尚无需求关联字段。
- [ ] 明确单条用例关联一个或多个需求的规则，并保存设计时使用的需求版本或内容快照，避免需求修改后丢失生成依据；覆盖用例导入、版本派生和合并时的关联保留。
- [ ] 后续在用例管理侧接入 generator：以审核后的用例及对应需求上下文为输入生成 Playwright 脚本，并记录用例与脚本的对应关系。该项暂不实现。
